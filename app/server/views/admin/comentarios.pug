extends partials/layout

block head
    title Comentarios

block content
    div(data-ng-app="dashboard")
        div
            include partials/navbar
        div.container-fluid
            div.row
                include partials/sidebar
                div.col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-2.col-xs-12.main
                    h1.page-header(style="padding-top: 5px") Comentarios
                    div.col-md-12.col-sm-12(data-ng-controller="ComentariosController")
                        table.table
                            thead
                                tr
                                    th Eliminar
                                    th Usuario
                                    th Comentario
                                    th Fecha
                                    th Validado
                            tbody(data-ng-repeat="comentario in comentarios")
                                tr.verEmail(data-ng-show="!comentario.validado", data-ng-cloak)
                                    td(style="font-weight: bold")
                                        span(data-ng-click="eliminarComentario(comentario)")
                                            i.fa.fa-trash-o
                                    td(style="font-weight: bold", data-ng-click="verComentario(comentario)") {{comentario.user}}
                                    td.mensajeEmail(style="font-weight: bold", data-ng-click="verComentario(comentario)") {{comentario.texto}}
                                    td(style="font-weight: bold", data-ng-click="verComentario(comentario)") {{comentario.fecha | date:'dd/MM/yyyy'}}
                                    td(style="font-weight: bold", data-ng-click="verComentario(comentario)") No
                                tr.verEmail(data-ng-show="comentario.validado", data-ng-cloak)
                                    td
                                        span(data-ng-click="eliminarComentario(comentario)")
                                            i.fa.fa-trash-o
                                    td(data-ng-click="verComentario(comentario)") {{comentario.user}}
                                    td.mensajeEmail(data-ng-click="verComentario(comentario)") {{comentario.texto}}
                                    td(data-ng-click="verComentario(comentario)") {{comentario.fecha | date:'dd/MM/yyyy'}}
                                    td(data-ng-click="verComentario(comentario)") Sí

                        div.col-xs-12.alert.alert-info(data-ng-show="comentarios.length == 0 || comentarios == null", data-ng-cloak)
                            | No hay comentarios.

                        include ../modals/eliminarRegistro




block scripts
    script(src="/js/jquery.js")
    script(src="/js/bootstrap.js")
    script(src="/js/angular.js")
    script(src='/js/angular/admin/dashboard.js')
    script(src="/js/angular/admin/comentarios.js")
    script(src="/js/angular/admin/navbar.js")
    script(src="/js/angular/admin/sidebar.js")
    script(src='/js/angular/ui-bootstrap-tpls-1.3.3.js')
